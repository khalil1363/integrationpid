spring.application.name=evaluation
# Load shared config from Config Server (8888). Optional so service can still start if config-server is down.
spring.config.import=optional:configserver:http://localhost:8888

# DB / port / eureka / uploads settings are served by config-server
# See: backend/microservices/config-repo/evaluation.properties