spring.application.name=user

# Load config from Config Server (8888)
# IMPORTANT: this requires config-server to be running and serving config-repo/user.properties
spring.config.import=configserver:http://localhost:8888,optional:classpath:application-local.properties

# All other settings are served by config-server:
# - backend/microservices/config-repo/user.properties
